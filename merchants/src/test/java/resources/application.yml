spring:
  application:
    name:Merchants
  datasource:
    url: jdbc:mysql://localhost:3306/passbook?useUnicode=true&characterEncoding=utf8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&&useSSL=false
    username: root
    password: 123
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: passbook
    listener:
      concurrency: 4

server:
  port: 9527

